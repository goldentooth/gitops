apiVersion: apps/v1
kind: Deployment
metadata:
  name: goldentooth-mcp
  namespace: goldentooth-mcp
  labels:
    app: goldentooth-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: goldentooth-mcp
  template:
    metadata:
      labels:
        app: goldentooth-mcp
    spec:
      serviceAccountName: goldentooth-mcp
      containers:
        - name: goldentooth-mcp
          image: registry.goldentooth.net/goldentooth-mcp:4-94b9a4fd1db8b38d45675b18fe4717f9180c09a0 # {"$imagepolicy": "flux-system:mcp"}
          ports:
            - name: dev
              containerPort: 8080
              protocol: TCP
            - name: auth
              containerPort: 8443
              protocol: TCP
          env:
            - name: GOLDENTOOTH_MCP_DEV
              value: "true"
            - name: RUST_LOG
              value: "info"
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
