---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wiz6
  namespace: wiz6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wiz6
  template:
    metadata:
      labels:
        app: wiz6
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: viewer
          image: ghcr.io/ndouglas/wiz6:sha-9441b624c88c27a5d463667729c26903241c86aa
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 1
            periodSeconds: 5
