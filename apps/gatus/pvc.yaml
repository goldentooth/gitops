---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-data
  namespace: gatus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: seaweedfs
  resources:
    requests:
      storage: 1Gi
