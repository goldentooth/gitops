---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: step-issuer
  namespace: flux-system
spec:
  interval: 30m
  targetNamespace: step-issuer
  chart:
    spec:
      chart: step-issuer
      version: "1.9.x"
      sourceRef:
        kind: HelmRepository
        name: step-issuer
        namespace: flux-system
  install:
    createNamespace: false
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    # kube-rbac-proxy image moved from gcr.io to registry.k8s.io
    kubeRBACproxy:
      image:
        repository: registry.k8s.io/kubebuilder/kube-rbac-proxy
        tag: v0.15.0

    # Replica count
    replicaCount: 1

    # Resource limits
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        memory: 128Mi

    # Prometheus metrics
    metrics:
      enabled: true

    # Webhook configuration
    webhook:
      enabled: true
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
        limits:
          memory: 64Mi
