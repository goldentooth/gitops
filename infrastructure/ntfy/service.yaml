---
apiVersion: v1
kind: Service
metadata:
  name: ntfy
  namespace: ntfy
  annotations:
    gatus.io/enabled: "true"
    gatus.io/path: /v1/health
spec:
  type: ClusterIP
  selector:
    app: ntfy
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
