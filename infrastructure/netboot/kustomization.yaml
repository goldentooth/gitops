---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - node-inventory.yaml
  - setup-boot-assets-script.yaml
  - grub-arm64-efi-configmap.yaml
  - matchbox-groups.yaml
  - matchbox-profiles.yaml
  # HA netboot stack @ 10.4.0.8 (kube-vip + dnsmasq + matchbox on the disk-booting Pi5
  # workers). Sole netboot path now. See ha/README.md + IP-ALLOCATION.md.
  - ha/kube-vip.yaml
  - ha/dnsmasq.yaml
  - ha/matchbox.yaml
  - ha/pdb.yaml
