---
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-config
  namespace: docker-registry
data:
  config.yml: |
    version: 0.1
    log:
      level: info
      fields:
        service: registry
    storage:
      s3:
        region: us-east-1
        bucket: docker-registry
        regionendpoint: http://seaweedfs-seaweedfs-s3.seaweedfs.svc.cluster.local:8333
      redirect:
        disable: true
      delete:
        enabled: true
      cache:
        blobdescriptor: inmemory
    http:
      addr: :5000
      headers:
        X-Content-Type-Options: [nosniff]
      tls:
        certificate: /certs/tls.crt
        key: /certs/tls.key
    health:
      storagedriver:
        enabled: true
        interval: 10s
        threshold: 3
