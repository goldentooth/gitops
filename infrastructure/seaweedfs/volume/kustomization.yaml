---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # DaemonSet running on all nodes with storage.seaweedfs/volume=true label
  # Uses hostPath to access Talos-managed USB SSDs at /var/mnt/usb
  - daemonset.yaml
