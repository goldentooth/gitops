---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # kube-prometheus-stack (HelmRepository + HelmRelease)
  # Installs full monitoring stack into monitoring namespace:
  # - Prometheus Operator, Prometheus, Grafana, Alertmanager
  # - Node Exporter, Kube-State-Metrics, ServiceMonitors
  - repository.yaml
  - operator.yaml
