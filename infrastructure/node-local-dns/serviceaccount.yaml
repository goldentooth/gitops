---
# The node-local cache only caches + forwards (no kubernetes plugin), so it needs
# no API access — a bare ServiceAccount with no RBAC is sufficient.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: node-local-dns
  namespace: kube-system
  labels:
    k8s-app: node-local-dns
